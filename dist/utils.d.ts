import { DataStorageModel } from "./models/data-storage.model";
export declare const makeObjectKey: (first: string, second: string) => string;
export declare const findRate: (first: string, second: string, data: DataStorageModel) => number;
